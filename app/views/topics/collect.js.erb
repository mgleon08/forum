$("#col").toggleClass("btn-warning")
$("#col").toggleClass("btn-default")

$("#col-name").text("<%= is_topic_collect_name(@topic) %>");
$("#cc").text("<%= topic_collect_user_name(@topic) %>");

$("#col-number").text(<%=@topic.topic_user_collects.count%>);
